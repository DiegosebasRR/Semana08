<Window x:Class="Semana07.Update"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Semana07"
        mc:Ignorable="d"
        Title="Update" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="txtName" Margin="10" Grid.Row="0" Width="200" />
        <TextBox x:Name="txtPrice" Margin="10" Grid.Row="1" Width="200" />
        <TextBox x:Name="txtStock" Margin="10" Grid.Row="2" Width="200" />
        <CheckBox x:Name="chkActive" Content="Active" Margin="10" Grid.Row="3" />
        <Button Content="Actualizar" Click="Button_Actualizar_Click" Margin="10" Grid.Row="4" Width="100" />
    </Grid>
</Window>
